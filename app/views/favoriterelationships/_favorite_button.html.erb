<% if current_user.favoriting?(micropost) %>
    <%= form_for(current_user.favoriterelationships.find_by(micropost_id: micropost.id), html: { method: :delete }) do |f| %>
      <%= hidden_field_tag :micropost_id, micropost.id %>
      <%= f.submit 'Unfavorite', class: 'btn btn-secondary btn-xs' %>
    <% end %>
  <% else %>
    <%= form_for(current_user.favoriterelationships.build) do |f| %>
      <%= hidden_field_tag :micropost_id, micropost.id %>
      <%= f.submit 'Favorite', class: 'btn btn-success btn-xs' %>
    <% end %>
<% end %>